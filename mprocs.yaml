procs:
  # client:
  #   cwd: packages/client
  #   shell: pnpm run dev
  contracts:
    cwd: packages/contracts
    shell: pnpm mud dev-contracts --rpc http://127.0.0.1:8545
  contracts-test:
    cwd: packages/contracts
    shell: pnpm run test
  anvil:
    cwd: packages/contracts
    shell: anvil --block-base-fee-per-gas 0 --block-time 2
